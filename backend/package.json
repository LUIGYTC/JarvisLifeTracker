{
  "name": "jarvis-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=24" },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --env-file=.env src/server.js",
    "test": "node --test test/*.test.js",
    "bootstrap": "node tools/discover-sub.js --discover-sub"
  },
  "dependencies": { "google-auth-library": "^10.0.0" }
}
